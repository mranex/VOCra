# Working Log

## Session

- Date: `2026-05-26`
- Project: `VoCRA`
- Full name: `Video OCR Subtitle Extractor`
- Stack direction: `Python 3.11 + PySide6`

## Da Lam

- Da doc va tong hop y tuong chinh tu `Ideal.md`.
- Da doc va tong hop architecture, stack, phase roadmap tu `total_plan.md`.
- Da tao thu muc `docs/` va file giu cho `docs/.gitkeep`.
- Da khoi tao file `working.md` de lam moc doi chieu cho cac phien tiep theo.
- Da doc `phases/phase1_scaffold.md` va bam scope phase 1.
- Da scaffold bo khung phase 1:
  - `main.py`
  - `vocra_core/`
  - `vocra_gui/`
  - `tools/README.md`
- Da tao `vocra_core/default_config.json`.
- Da implement `vocra_core/project_manager.py` voi cac ham:
  - `create_project()`
  - `load_project()`
  - `update_status()`
  - `get_cache_path()`
  - `save_progress()`
- Da verify:
  - `python main.py` chay khong crash
  - `create_project()` tao dung thu muc cache va `progress.json`
  - `load_project()` doc lai du lieu dung
  - `update_status()` ghi lai trang thai dung
  - cac path cache trong `progress.json` la path tuong doi theo `project_dir`
- Da doc `phases/phase2_video_pipeline.md` va implement:
  - `vocra_core/frame_extractor.py`
  - `vocra_core/cropper.py`
  - `vocra_core/draft_ocr_providers.py`
  - `vocra_core/draft_ocr.py`
- Da verify phase 2 bang bai test end-to-end voi video mau:
  - `extract_frames()` tao frame PNG + `timestamp.json`
  - `crop_frames()` tao anh crop theo subtitle region
  - `run_draft_ocr()` tao `ocr_og.json`
  - ca 3 buoc deu ho tro resume/skip du lieu da co
  - callback progress duoc goi trong moi buoc
  - `progress.json` duoc update status sau moi step
- Da doc `phases/phase3_segment_preprocess.md` va implement:
  - `vocra_core/segmenter.py`
  - `vocra_core/preprocessor.py`
- Da verify phase 3 bang bai test co blank frame, OCR noise, va image nen toi:
  - `build_segments()` bo qua blank frame, gom cac dong gan giong nhau, va tao `segments.json`
  - `preprocess_representatives()` chi tao anh cho `represent_image`
  - anh preprocess la grayscale, contrast-enhanced, upscale 2x, va auto-invert khi nen toi
  - ca 2 buoc deu ho tro resume/skip du lieu da co
  - `progress.json` duoc update cho `segments_done` va `preprocessed_done`
- Da doc ky `phases/phase4_backends.md`, doi chieu lai voi `Ideal.md` va `total_plan.md`, va khoa uu tien backend nhu sau:
  - Final OCR: `llama.cpp local -> Chrome Lens -> OpenAI-compatible API`
  - Translator: `OpenAI-compatible API -> local`
- Da cap nhat `vocra_core/default_config.json` de Final OCR mac dinh la `llama_cpp`, Translator mac dinh van la `openai_compatible`.
- Da implement phase 4:
  - `vocra_core/text_cleaner.py`
  - `vocra_core/final_ocr/base.py`
  - `vocra_core/final_ocr/openai_compatible.py`
  - `vocra_core/final_ocr/llama_server_manager.py`
  - `vocra_core/final_ocr/chrome_lens.py`
  - `vocra_core/final_ocr/provider_factory.py`
  - `vocra_core/run_final_ocr.py`
  - `vocra_core/translator/base.py`
  - `vocra_core/translator/openai_compatible.py`
  - `vocra_core/translator/llama_local.py`
  - `vocra_core/translator/provider_factory.py`
  - `vocra_core/run_translator.py`
  - `vocra_core/exporter.py`
- Da verify phase 4 bang mock local server:
  - Final OCR provider OpenAI-compatible/llama.cpp-style request-response chay duoc
  - `run_final_ocr()` resume duoc va save `ocr_fn.json` sau moi item
  - `LlamaServerManager` tao `run_server.bat` va health check duoc
  - Chrome Lens backend tra loi loi ro rang neu chua co dependency
  - Translator OpenAI-compatible chay batch, resume duoc, va save `translation.json` sau moi batch
  - Exporter xuat duoc `SRT`, `ASS`, `TXT` voi timestamp dung format
- Da noi cau hinh Final OCR sang moi truong llama.cpp that trong workspace:
  - `llama_cpp_dir`: `tools/`
  - `model_path`: `models/paddleocr_vl/model.gguf`
  - `mmproj_path`: `models/paddleocr_vl/mmproj.gguf`
  - `prompt`: `OCR:`
- Da sua `LlamaServerManager` de path tuong doi duoc resolve theo workspace root, tranh loi khi `run_server.bat` `cd` vao `tools/`.
- Da tao `servers/run_server.bat` tu cau hinh hien tai de san sang test local server that.
- Da doc `phases/phase5_gui.md` va implement GUI phase 5 voi PySide6:
  - `vocra_gui/main_window.py`
  - `vocra_gui/scene_setup.py`
  - `vocra_gui/scene_process.py`
  - `vocra_gui/scene_translator.py`
  - `vocra_gui/scene_export.py`
  - `vocra_gui/scene_config.py`
  - `vocra_gui/workers.py`
  - `vocra_gui/styles/theme.qss`
  - `vocra_gui/widgets/video_preview.py`
  - `vocra_gui/widgets/crop_overlay.py`
  - `vocra_gui/widgets/log_panel.py`
  - `vocra_gui/widgets/subtitle_table.py`
  - `vocra_gui/widgets/progress_bar.py`
- GUI da noi voi core that:
  - Scene Setup tao project moi qua `create_project()`
  - Scene Process goi `extract_frames()`, `crop_frames()`, `run_draft_ocr()`, `build_segments()`, `preprocess_representatives()`, `run_final_ocr()`
  - Scene Translator goi `run_translation()`
  - Scene Export goi `export_srt()`, `export_ass()`, `export_txt()`
  - Scene Config doc/ghi truc tiep vao `progress.json`
- Da them `TaskWorker` dua cac task chay trong `QThread` de tranh block GUI.
- Da smoke test GUI:
  - `compileall` cho `main.py` va toan bo `vocra_gui/`
  - launch app bang `QT_QPA_PLATFORM=offscreen`
  - tao project mau va `window.load_project()` thanh cong
  - confirm Scene Config load prompt `OCR:` dung tu config hien tai
- Da fix loi khi vua chon video co codec AV1:
  - bo viec dung `cv2.VideoCapture()` ngay trong `VideoPreview.set_source()`
  - `video_preview._load_video_size()` nay dung `ffprobe` de doc width/height, khong can decode frame
  - `video_preview.capture_current_frame()` nay dung `ffmpeg` xuat frame tam, tranh nhanh hardware decode AV1 cua OpenCV
- Da them fix cap app cho Qt Multimedia / FFmpeg backend trong `main.py`:
  - set `QT_FFMPEG_DECODING_HW_DEVICE_TYPES=,` de tat toan bo hardware decoding backend
  - set `QT_DISABLE_HW_TEXTURES_CONVERSION=1` de giam them rui ro render/hardware texture path
  - ep `QT_MEDIA_BACKEND=ffmpeg` de giu codec coverage cho AV1 la target that cua app
  - khong dung huong `QT_MEDIA_BACKEND=windows` nua vi huong do co the lam mat playback cho video target
  - muc tieu hien tai: van dung FFmpeg backend, nhung buoc no decode bang software thay vi nhay vao `d3d11va`
- Da thay doi huong fix lon cho Scene Setup video preview:
  - bo hẳn `QMediaPlayer + QVideoWidget` trong `vocra_gui/widgets/video_preview.py`
  - thay bang preview tu decode bang `OpenCV + FFmpeg` voi `CAP_FFMPEG`
  - mo capture voi `CAP_PROP_HW_ACCELERATION=VIDEO_ACCELERATION_NONE` de ep software decode
  - ho tro play/pause, seek slider, current frame capture, va video metadata tren chinh widget custom moi
- Da test truc tiep voi file `C:\\Users\\Admin\\Downloads\\test.mp4`:
  - open thanh cong
  - doc frame dau thanh cong
  - seek den `2000 ms` thanh cong
  - lay current frame thanh cong
- Da nang cap UX crop o Scene Setup:
  - viền crop khi dang ve/unlocked doi sang vang de nguoi dung thay ro vung dang chon
  - them nut `Lock Crop` trong Scene Setup
  - sau khi lock, viền crop doi sang xanh la va trang thai hien `locked and ready`
  - `Create Project` gio yeu cau vung crop da duoc lock
  - overlay luu crop theo toa do video that de resize/load lai project khong mat vung
- Da fix loi khoi tao Draft OCR tren may hien tai:
  - `PaddleOCR 3.5.0` bi fail o nhanh `doc_orientation_classify` mac dinh voi loi `No valid PaddlePaddle model found`
  - `vocra_core/draft_ocr_providers.py` nay khoi tao `PaddleOCR` theo huong draft OCR toi gian:
    - `use_doc_orientation_classify=False`
    - `use_doc_unwarping=False`
    - `use_textline_orientation=False`
  - da verify lai rang provider init thanh cong tren chinh may nay
- Da fix tiep API mismatch cua `PaddleOCR 3.5.0`:
  - code cu goi `.ocr(image_path, cls=True)` theo API version cu
  - code moi chuyen sang `.predict(image_path)` va parse `rec_texts` + `rec_scores`
  - da verify:
    - `provider.recognize()` chay duoc tren anh crop that cua project `projects/test`
    - `run_draft_ocr()` chay duoc tren mini project lay tu cropped images that
- Da bo sung fallback subprocess cho Draft OCR:
  - neu process GUI khong import duoc `paddle`, `vocra_core/draft_ocr.py` se tu tim mot Python interpreter co `paddle + paddleocr`
  - sau do chay helper `vocra_core/draft_ocr_cli.py` de OCR o process rieng
  - da verify helper subprocess chay duoc va tra progress/result tren mini project that
- Da doi chien luoc Draft OCR:
  - subprocess helper gio la duong chay chinh cua `run_draft_ocr()`
  - in-process OCR chi con la duong du phong ky thuat neu helper subprocess khong kha dung
  - da verify lai `run_draft_ocr()` tren mini project va xac nhan no that su di qua helper process
- Da dong bo memory/cache strategy cua Final OCR `llama_cpp` theo huong `manga-translator`:
  - them `vocra_core/final_ocr/llama_cache.py` de goi `/slots` va `POST /slots/{id}?action=erase`
  - them `vocra_core/final_ocr/paddleocr_vl.py` lam provider rieng cho `PaddleOCR-VL` local
  - `provider_factory.py` gio tach ro:
    - `llama_cpp` -> `PaddleOCRVLLocalProvider`
    - `openai_compatible` -> `OpenAICompatibleOCRProvider`
    - `chrome_lens` -> `ChromeLensOCRProvider`
  - `PaddleOCRVLLocalProvider` gui payload theo format:
    - `image_url` truoc
    - `text: OCR:` sau
  - provider local nay da hoc theo logic `manga-translator`:
    - `repeat_penalty`
    - `repeat_last_n`
    - retry neu output bi loop/degenerate
    - clean output OCR va bo prefix/list marker
  - `run_final_ocr()` gio clear slot cache best-effort theo `clear_cache_interval` sau khi OCR item bang provider local
  - `vocra_core/final_ocr/llama_server_manager.py` gio build `run_server.bat` kem:
    - `--no-cache-prompt`
    - `--cache-ram 0`
    - `--no-cache-idle-slots`
  - `vocra_core/default_config.json` da duoc bo sung:
    - `repeat_penalty: 1.2`
    - `repeat_last_n: -1`
    - `max_retries: 3`
    - `clear_cache_interval: 1`
  - da cap nhat ca `servers/run_server.bat` hien co de khop voi code moi, tranh test nham file bat cu
- Da verify core memory/cache OCR final bang mock local server:
  - `LlamaServerManager.build_bat_content()` co du 3 co memory flags
  - `run_final_ocr()` gui request dung format `image -> text OCR:`
  - retry OCR xay ra khi response dau bi loop/degenerate
  - `/slots` duoc query va slot erase duoc goi that sau item
  - `ocr_fn.json` duoc luu dung va `ocr_final_done` chuyen `true`
- Da them knob config de chuan bi cho batch/parallel OCR sau nay:
  - `final_ocr.parallel_slots`
  - mac dinh = `1`
  - da dua len GUI Config va `LlamaServerManager`
  - `run_server.bat` gio se sinh them `--parallel <N>`
  - ban dau knob nay moi chi mo slot o server
- Da fix tiep de `parallel_slots` duoc dung that trong app:
  - `run_final_ocr()` gio khong con OCR final theo vong `for` tuan tu nua
  - cac segment pending duoc chay theo tung wave song song voi `ThreadPoolExecutor`
  - kich thuoc wave = `final_ocr.parallel_slots`
  - callback/progress van duoc cap nhat sau moi item hoan tat
  - `ocr_fn.json` van save sau moi item de giu kha nang resume/debug
  - clear llama slot cache gio duoc goi sau moi wave khi dat nguong `clear_cache_interval`, tranh quet slot trong luc request cung wave con dang chay
- Da verify concurrent Final OCR bang mock server:
  - `parallel_slots = 3`
  - 3 request OCR chay dong thoi that (`max_active = 3`)
  - tong thoi gian xap xi 1 giay cho 3 item sleep 1 giay, xac nhan khong con la flow tuan tu
  - slot cache clear duoc goi 1 lan sau wave
- Da fix nhom meta-response cua Final OCR:
  - neu model tra ve cau kieu:
    - `The image is too blurry to recognize any text content.`
    - `Unable to recognize any text ...`
    - `No readable text ...`
  - app se khong coi do la subtitle that nua
  - `text_cleaner.py` gio co:
    - `is_meta_ocr_response()`
    - `sanitize_ocr_text()`
  - khi Final OCR gap meta-response:
    - `text` bi doi thanh chuoi rong
    - `raw_ocr_text` van duoc giu lai de debug
    - `rejection_reason = meta_response`
  - `run_translator.py` cung bo qua khong dich cac item rong/meta-response
  - `scene_export.py` cung chan luon meta-response khi load table/export, de cache cu it gay sub rac hon
- Da verify nhanh bang cac mau text:
  - `The image is too blurry to recognize any text content.` -> bi sanitize thanh rong
  - `OCR: The image is too blurry to recognize any text content.` -> bi sanitize thanh rong
  - `Unable to recognize any text in the image.` -> bi sanitize thanh rong
  - text subtitle binh thuong van giu nguyen
- Da chuyen `Save Config` sang global app config:
  - them `vocra_core/app_config.py`
  - global config duoc luu ngoai repo, mac dinh tai:
    - Windows: `%APPDATA%\\VoCRA\\settings.json`
    - Windows secret: `%APPDATA%\\VoCRA\\secrets.json`
  - co ho tro `VOCRA_CONFIG_DIR` de override duong dan khi can test
- Global config hien nay bao gom:
  - `draft_ocr`
  - `final_ocr`
  - `translator`
- API key translator da duoc tach rieng:
  - khong luu trong `progress.json`
  - khong luu trong workspace/repo
  - `settings.json` khong chua secret
  - `secrets.json` moi chua `translator.api_key`
  - `create_translator()` se tu fallback ve global secret neu config runtime khong co `api_key`
- `ConfigScene` da doi hanh vi:
  - `Save Config` gio save global cho toan app
  - co the mo app len va save config du khong co project nao dang mo
  - co ghi chu ro trong UI rang API key duoc luu ngoai repo
- `VoCRAMainWindow` da load global config luc startup:
  - scene Config duoc refresh ngay ca khi chua mo project
  - khi create/load project, non-secret global config se duoc sync vao project progress de core pipeline dung dung setting hien tai
  - neu project cu tung lo `translator.api_key` trong `progress.json`, sync moi se go bo key do khoi file project
- Da verify nhanh:
  - global `settings.json` + `secrets.json` duoc tao ra dung
  - `settings.json` khong chua API key
  - `secrets.json` co API key
  - merge vao project progress khong giu `translator.api_key`
  - translator provider fallback lay duoc API key tu global secret

## Huong San Pham Da Nam

- App OCR subtitle tu video theo pipeline tach tung buoc.
- Moi buoc can co cache file rieng de de debug, resume, va chay lai tung phan.
- Cache chinh gom:
  - `progress.json`
  - `timestamp.json`
  - `ocr_og.json`
  - `segments.json`
  - `ocr_fn.json`
  - `translation.json`
- GUI du kien co 5 scene:
  - Setup
  - Process
  - Translator
  - Export
  - Config

## Viec Con Ton

- Bo sung test/verification voi backend that:
  - PaddleOCR draft environment
  - llama.cpp local server
  - Chrome Lens neu duoc su dung
  - translator API that
- Manual test full workflow trong GUI:
  - video preview + pause + crop overlay
  - Prepare / Draft OCR / Final OCR
  - translation batches
  - subtitle table edit + save edits
  - export file that

## Bug Va Rui Ro Hien Tai

- Workspace hien chua la git repository tai `C:\Nghich\vocra`.
- `Ideal.md` hien thi loi ky tu trong terminal. Kha nang cao la file van la UTF-8, nhung PowerShell/code page hien tai dang doc hien thi sai.
- Smoke test `PaddleOCRDraftProvider` that bai tren moi truong local hien dang fail do model/runtime cua PaddleOCR. Pipeline orchestration van on, nhung can xu ly moi truong Paddle neu muon OCR that ngay trong may nay.
- Tai lieu phase 3 hien co mo ta chua dong nhat ve `represent_image`, nhung ban code hien tai dang theo huong lay anh dau tien cua group.
- Chrome Lens backend da co wrapper va graceful error, nhung chua verify voi package `chrome-lens-py` that tren may nay.
- Backend phase 4 da duoc verify bang mock server; can test lai voi server/API that khi setup moi truong OCR/translation hoan chinh.
- Final OCR local da hoc them chien luoc xoa llama slot cache, nhung van chua test voi `llama-server.exe + paddleocr-vl` that tren may nay; can test desktop that de xem:
  - `/slots` co duoc binary hien tai ho tro hay khong
  - toc do co bi giam qua nhieu khi `clear_cache_interval = 1` hay can nang len `2/5/10`
  - `parallel_slots` bao nhieu la diem ngot tren may that cua user
  - model `paddleocr-vl` co con lap output o video subtitle that hay khong
- co can mo rong them danh sach meta-response neu model that con tra ve cac cau tu choi/bao loi khac
- Can test desktop that sau khi restart app:
  - config global co tu load lai dung hay khong
  - `Create .bat` co dung settings global moi nhat hay khong
  - translation API that co dung duoc key tu secret file hay khong
- GUI phase 5 da smoke test launch va load project, nhung chua manual test het cac tuong tac multimedia that tren desktop:
  - crop overlay mapping voi video that
  - worker cancellation trong translation o batch lon
  - inline edit + export sau khi sua subtitle
- Video preview khong con dua vao Qt Multimedia nua, nen cac log `qt.multimedia.ffmpeg` khong con la huong chinh can debug cho Scene Setup playback.
- Manual test tiep theo can tap trung vao:
  - mau viền vang/xanh co hien ro tren desktop that hay khong
  - nut `Lock Crop` co dung flow thao tac mong muon cua user hay can them `Reset Crop`
  - Draft OCR full run sau khi provider da init duoc, de xem toc do va output OCR thuc te

## Ke Hoach Tiep Theo

1. Manual test toan bo app trong GUI voi du lieu/video that.
2. Uu tien test `Final OCR` voi local `llama.cpp + paddleocr-vl` that:
   - check health server
   - run OCR final
   - quan sat RAM/cache va log slot clear
3. Fix cac bug multimedia, OCR backend that, va export neu phat sinh khi test desktop.
4. Sau do moi tinh chinh UX, validation, va polish.

## Ghi Chu

- Yeu cau `docs/*` duoc hieu la tao san thu muc `docs/` cho tai lieu noi bo.
- Ten file y tuong trong workspace hien dang la `Ideal.md` chu khong phai `ideal.md`.
